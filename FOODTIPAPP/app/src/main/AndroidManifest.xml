<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodtip" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FOODTIP" >
        <activity android:name=".View.Main_View"
            android:exported="false"/>
        <activity
            android:name=".View.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.User.MyCousine" />
        <activity android:name=".View.User.MyFavorite" />
        <activity android:name=".View.User.MyHistory" />
        <activity android:name=".View.User.MySetting" />
        <activity android:name=".View.Search_View" />
        <activity android:name=".View.Register_View" />
    </application>

</manifest>